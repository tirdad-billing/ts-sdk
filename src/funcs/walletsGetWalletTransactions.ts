/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { TirdadCore } from "../core.js";
import { encodeFormQuery, encodeSimple } from "../lib/encodings.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../sdk/models/httpclienterrors.js";
import * as models from "../sdk/models/index.js";
import { ResponseValidationError } from "../sdk/models/responsevalidationerror.js";
import { SDKValidationError } from "../sdk/models/sdkvalidationerror.js";
import { TirdadError } from "../sdk/models/tirdaderror.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * Get wallet transactions
 *
 * @remarks
 * Use when showing transaction history for a wallet (e.g. credit/debit log or audit). Returns a paginated list; supports limit, offset, and filters.
 */
export function walletsGetWalletTransactions(
  client: TirdadCore,
  request: models.GetWalletTransactionsRequest,
  options?: RequestOptions,
): APIPromise<
  Result<
    models.ListWalletTransactionsResponse,
    | models.ErrorsErrorResponse
    | TirdadError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    request,
    options,
  ));
}

async function $do(
  client: TirdadCore,
  request: models.GetWalletTransactionsRequest,
  options?: RequestOptions,
): Promise<
  [
    Result<
      models.ListWalletTransactionsResponse,
      | models.ErrorsErrorResponse
      | TirdadError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const parsed = safeParse(
    request,
    (value) => models.GetWalletTransactionsRequest$outboundSchema.parse(value),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = null;

  const pathParams = {
    id: encodeSimple("id", payload.idPathParameter, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path = pathToFunc("/wallets/{id}/transactions")(pathParams);

  const query = encodeFormQuery({
    "created_by": payload.created_by,
    "credits_available_gt": payload.credits_available_gt,
    "end_time": payload.end_time,
    "expand": payload.expand,
    "expiry_date_after": payload.expiry_date_after,
    "expiry_date_before": payload.expiry_date_before,
    "id": payload.idQueryParameter,
    "limit": payload.limit,
    "offset": payload.offset,
    "order": payload.order,
    "priority": payload.priority,
    "reference_id": payload.reference_id,
    "reference_type": payload.reference_type,
    "start_time": payload.start_time,
    "status": payload.status,
    "transaction_reason": payload.transaction_reason,
    "transaction_status": payload.transaction_status,
    "type": payload.type,
  });

  const headers = new Headers(compactMap({
    Accept: "application/json",
  }));

  const secConfig = await extractSecurity(client._options.apiKeyAuth);
  const securityInput = secConfig == null ? {} : { apiKeyAuth: secConfig };
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "getWalletTransactions",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: client._options.apiKeyAuth,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    query: query,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode: number) =>
      matchStatusCode({ status: statusCode } as Response, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    models.ListWalletTransactionsResponse,
    | models.ErrorsErrorResponse
    | TirdadError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(200, models.ListWalletTransactionsResponse$inboundSchema),
    M.jsonErr([400, 404], models.ErrorsErrorResponse$inboundSchema),
    M.jsonErr(500, models.ErrorsErrorResponse$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
