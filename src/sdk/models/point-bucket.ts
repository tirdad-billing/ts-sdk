/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type PointBucket = {
  bucketId?: string | undefined;
  priceId?: string | undefined;
};

/** @internal */
export const PointBucket$inboundSchema: z.ZodMiniType<PointBucket, unknown> = z
  .pipe(
    z.object({
      bucket_id: types.optional(types.string()),
      price_id: types.optional(types.string()),
    }),
    z.transform((v) => {
      return remap$(v, {
        "bucket_id": "bucketId",
        "price_id": "priceId",
      });
    }),
  );

export function pointBucketFromJSON(
  jsonString: string,
): SafeParseResult<PointBucket, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PointBucket$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PointBucket' from JSON`,
  );
}
