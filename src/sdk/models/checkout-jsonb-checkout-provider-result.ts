/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import {
  PaymentAction,
  PaymentAction$inboundSchema,
} from "./payment-action.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type CheckoutJSONBCheckoutProviderResult = {
  /**
   * ExpiresAt is the provider URL expiry. When set and earlier than the session expiry,
   *
   * @remarks
   * executeCheckoutAction tightens the session expiry to match.
   */
  expiresAt?: Date | undefined;
  nextAction?: PaymentAction | undefined;
  /**
   * ProviderMetadata holds provider-specific data not needed for business logic.
   */
  providerMetadata?: { [k: string]: string } | undefined;
  /**
   * ProviderPaymentIntentID is the provider-side charge/intent ID.
   *
   * @remarks
   * Stripe returns this at link creation (pi_xxx); others populate it from the webhook payload.
   */
  providerPaymentIntentId?: string | undefined;
  /**
   * ProviderSessionID is stored in EntityIntegrationMapping at link creation.
   *
   * @remarks
   *   Stripe:   Checkout Session ID  (cs_xxx)
   *   Razorpay: Payment Link ID      (plink_xxx)
   *   Nomod:    Payment Link ID      (NOTE: webhook uses Charge ID; look up by PaymentLinkID field)
   *   Moyasar:  Payment ID
   */
  providerSessionId?: string | undefined;
};

/** @internal */
export const CheckoutJSONBCheckoutProviderResult$inboundSchema: z.ZodMiniType<
  CheckoutJSONBCheckoutProviderResult,
  unknown
> = z.pipe(
  z.object({
    expires_at: types.optional(types.date()),
    next_action: types.optional(PaymentAction$inboundSchema),
    provider_metadata: types.optional(z.record(z.string(), types.string())),
    provider_payment_intent_id: types.optional(types.string()),
    provider_session_id: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "expires_at": "expiresAt",
      "next_action": "nextAction",
      "provider_metadata": "providerMetadata",
      "provider_payment_intent_id": "providerPaymentIntentId",
      "provider_session_id": "providerSessionId",
    });
  }),
);

export function checkoutJSONBCheckoutProviderResultFromJSON(
  jsonString: string,
): SafeParseResult<CheckoutJSONBCheckoutProviderResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      CheckoutJSONBCheckoutProviderResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CheckoutJSONBCheckoutProviderResult' from JSON`,
  );
}
