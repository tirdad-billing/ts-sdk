/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { MeterMeter, MeterMeter$inboundSchema } from "./metermeter.js";
import { SDKValidationError } from "./sdkvalidationerror.js";

export type MatchedMeter = {
  eventName?: string | undefined;
  meter?: MeterMeter | undefined;
  meterId?: string | undefined;
};

/** @internal */
export const MatchedMeter$inboundSchema: z.ZodType<
  MatchedMeter,
  z.ZodTypeDef,
  unknown
> = z.object({
  event_name: types.optional(types.string()),
  meter: types.optional(MeterMeter$inboundSchema),
  meter_id: types.optional(types.string()),
}).transform((v) => {
  return remap$(v, {
    "event_name": "eventName",
    "meter_id": "meterId",
  });
});

export function matchedMeterFromJSON(
  jsonString: string,
): SafeParseResult<MatchedMeter, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MatchedMeter$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MatchedMeter' from JSON`,
  );
}
