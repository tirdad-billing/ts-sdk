/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type CustomCurrencyLineItem = {
  amount?: string | undefined;
  invoiceLevelDiscount?: string | undefined;
  lineItemDiscount?: string | undefined;
  prepaidCreditsApplied?: string | undefined;
};

/** @internal */
export const CustomCurrencyLineItem$inboundSchema: z.ZodMiniType<
  CustomCurrencyLineItem,
  unknown
> = z.pipe(
  z.object({
    amount: types.optional(types.string()),
    invoice_level_discount: types.optional(types.string()),
    line_item_discount: types.optional(types.string()),
    prepaid_credits_applied: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "invoice_level_discount": "invoiceLevelDiscount",
      "line_item_discount": "lineItemDiscount",
      "prepaid_credits_applied": "prepaidCreditsApplied",
    });
  }),
);

export function customCurrencyLineItemFromJSON(
  jsonString: string,
): SafeParseResult<CustomCurrencyLineItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CustomCurrencyLineItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CustomCurrencyLineItem' from JSON`,
  );
}
