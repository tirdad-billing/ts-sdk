/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type CustomCurrency = {
  amountDue?: string | undefined;
  code?: string | undefined;
  /**
   * fiat per 1 unit of Code; the live factor while draft, frozen at finalization
   */
  rate?: string | undefined;
  subtotal?: string | undefined;
  total?: string | undefined;
  totalDiscount?: string | undefined;
  totalPrepaidCreditsApplied?: string | undefined;
  totalTax?: string | undefined;
};

/** @internal */
export const CustomCurrency$inboundSchema: z.ZodMiniType<
  CustomCurrency,
  unknown
> = z.pipe(
  z.object({
    amount_due: types.optional(types.string()),
    code: types.optional(types.string()),
    rate: types.optional(types.string()),
    subtotal: types.optional(types.string()),
    total: types.optional(types.string()),
    total_discount: types.optional(types.string()),
    total_prepaid_credits_applied: types.optional(types.string()),
    total_tax: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "amount_due": "amountDue",
      "total_discount": "totalDiscount",
      "total_prepaid_credits_applied": "totalPrepaidCreditsApplied",
      "total_tax": "totalTax",
    });
  }),
);

export function customCurrencyFromJSON(
  jsonString: string,
): SafeParseResult<CustomCurrency, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CustomCurrency$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CustomCurrency' from JSON`,
  );
}
