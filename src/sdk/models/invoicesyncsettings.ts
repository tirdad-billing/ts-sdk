/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { BillingPeriod, BillingPeriod$inboundSchema } from "./billingperiod.js";
import { SDKValidationError } from "./sdkvalidationerror.js";

export type InvoiceSyncSettings = {
  normalizeFixedTo?: BillingPeriod | undefined;
};

/** @internal */
export const InvoiceSyncSettings$inboundSchema: z.ZodType<
  InvoiceSyncSettings,
  z.ZodTypeDef,
  unknown
> = z.object({
  normalize_fixed_to: types.optional(BillingPeriod$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "normalize_fixed_to": "normalizeFixedTo",
  });
});

export function invoiceSyncSettingsFromJSON(
  jsonString: string,
): SafeParseResult<InvoiceSyncSettings, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InvoiceSyncSettings$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InvoiceSyncSettings' from JSON`,
  );
}
