/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import {
  CustomerInvoiceSummary,
  CustomerInvoiceSummary$inboundSchema,
} from "./customer-invoice-summary.js";
import { SDKValidationError } from "./sdk-validation-error.js";

export type CustomerMultiCurrencyInvoiceSummary = {
  /**
   * customer_id is the unique identifier of the customer
   */
  customerId?: string | undefined;
  /**
   * default_currency is the primary currency for this customer
   */
  defaultCurrency?: string | undefined;
  /**
   * summaries contains the invoice summaries for each currency
   */
  summaries?: Array<CustomerInvoiceSummary> | undefined;
};

/** @internal */
export const CustomerMultiCurrencyInvoiceSummary$inboundSchema: z.ZodMiniType<
  CustomerMultiCurrencyInvoiceSummary,
  unknown
> = z.pipe(
  z.object({
    customer_id: types.optional(types.string()),
    default_currency: types.optional(types.string()),
    summaries: types.optional(z.array(CustomerInvoiceSummary$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "customer_id": "customerId",
      "default_currency": "defaultCurrency",
    });
  }),
);

export function customerMultiCurrencyInvoiceSummaryFromJSON(
  jsonString: string,
): SafeParseResult<CustomerMultiCurrencyInvoiceSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      CustomerMultiCurrencyInvoiceSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CustomerMultiCurrencyInvoiceSummary' from JSON`,
  );
}
