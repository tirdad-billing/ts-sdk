/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";
import {
  TaxExemptionSummary,
  TaxExemptionSummary$inboundSchema,
} from "./tax-exemption-summary.js";

export type TaxSummary = {
  exclusiveTax?: string | undefined;
  exemption?: TaxExemptionSummary | undefined;
  inclusiveTax?: string | undefined;
  totalTax?: string | undefined;
};

/** @internal */
export const TaxSummary$inboundSchema: z.ZodMiniType<TaxSummary, unknown> = z
  .pipe(
    z.object({
      exclusive_tax: types.optional(types.string()),
      exemption: types.optional(TaxExemptionSummary$inboundSchema),
      inclusive_tax: types.optional(types.string()),
      total_tax: types.optional(types.string()),
    }),
    z.transform((v) => {
      return remap$(v, {
        "exclusive_tax": "exclusiveTax",
        "inclusive_tax": "inclusiveTax",
        "total_tax": "totalTax",
      });
    }),
  );

export function taxSummaryFromJSON(
  jsonString: string,
): SafeParseResult<TaxSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaxSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaxSummary' from JSON`,
  );
}
