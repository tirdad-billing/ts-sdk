/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "./sdkvalidationerror.js";
import {
  TenantBillingDetails,
  TenantBillingDetails$inboundSchema,
} from "./tenantbillingdetails.js";

export type TenantResponse = {
  billingDetails?: TenantBillingDetails | undefined;
  createdAt?: Date | undefined;
  id?: string | undefined;
  metadata?: { [k: string]: string } | undefined;
  name?: string | undefined;
  status?: string | undefined;
  updatedAt?: Date | undefined;
};

/** @internal */
export const TenantResponse$inboundSchema: z.ZodType<
  TenantResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  billing_details: types.optional(TenantBillingDetails$inboundSchema),
  created_at: types.optional(types.date()),
  id: types.optional(types.string()),
  metadata: types.optional(z.record(types.string())),
  name: types.optional(types.string()),
  status: types.optional(types.string()),
  updated_at: types.optional(types.date()),
}).transform((v) => {
  return remap$(v, {
    "billing_details": "billingDetails",
    "created_at": "createdAt",
    "updated_at": "updatedAt",
  });
});

export function tenantResponseFromJSON(
  jsonString: string,
): SafeParseResult<TenantResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TenantResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TenantResponse' from JSON`,
  );
}
