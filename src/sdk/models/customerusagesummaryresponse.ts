/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import {
  BillingPeriodInfo,
  BillingPeriodInfo$inboundSchema,
} from "./billingperiodinfo.js";
import {
  FeatureUsageSummary,
  FeatureUsageSummary$inboundSchema,
} from "./featureusagesummary.js";
import {
  PaginationResponse,
  PaginationResponse$inboundSchema,
} from "./paginationresponse.js";
import { SDKValidationError } from "./sdkvalidationerror.js";

export type CustomerUsageSummaryResponse = {
  customerId?: string | undefined;
  features?: Array<FeatureUsageSummary> | undefined;
  pagination?: PaginationResponse | undefined;
  period?: BillingPeriodInfo | undefined;
};

/** @internal */
export const CustomerUsageSummaryResponse$inboundSchema: z.ZodType<
  CustomerUsageSummaryResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  customer_id: types.optional(types.string()),
  features: types.optional(z.array(FeatureUsageSummary$inboundSchema)),
  pagination: types.optional(PaginationResponse$inboundSchema),
  period: types.optional(BillingPeriodInfo$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "customer_id": "customerId",
  });
});

export function customerUsageSummaryResponseFromJSON(
  jsonString: string,
): SafeParseResult<CustomerUsageSummaryResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CustomerUsageSummaryResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CustomerUsageSummaryResponse' from JSON`,
  );
}
