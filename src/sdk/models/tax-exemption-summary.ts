/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "./sdk-validation-error.js";
import {
  TaxExemptionReasonCode,
  TaxExemptionReasonCode$inboundSchema,
} from "./tax-exemption-reason-code.js";

export type TaxExemptionSummary = {
  reason?: string | undefined;
  reasonCode?: TaxExemptionReasonCode | undefined;
};

/** @internal */
export const TaxExemptionSummary$inboundSchema: z.ZodMiniType<
  TaxExemptionSummary,
  unknown
> = z.pipe(
  z.object({
    reason: types.optional(types.string()),
    reason_code: types.optional(TaxExemptionReasonCode$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "reason_code": "reasonCode",
    });
  }),
);

export function taxExemptionSummaryFromJSON(
  jsonString: string,
): SafeParseResult<TaxExemptionSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaxExemptionSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaxExemptionSummary' from JSON`,
  );
}
